texto="esto es una prueba"
fecha="16/10/2000"
"prueba" in texto #mira si esta la palabra en la string
texto.split() #hace el texto una lista
fecha.split("/") #corta donde aparece el caracter
help(str.replace) #nos dice para que sirve
#str.replace sirve para remplazar un caracter por otro
str.replace(texto,"es","era")
